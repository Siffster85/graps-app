
export type ErrorResponse = {
    message: string;
    };

export enum Roles {
    Admin = "ADMIN",
    Member = "MEMBER",
    }

export enum ButtonColor {
        Primary = "primary",
        Secondary = "secondary",
        Inherit = "inherit",
    }